# Runs backend and frontend services together

services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    env_file:
      - .env
      
    # Henke
    # ports:
    #   - "8002:8002"

    # Mona
    # ports:
    #   - "8003:8003"

    # Orhan
    ports:
      - "8001:8001"

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"